{% extends 'archive/index.html.twig' %}

{% block breadcrumb %}

    {% if currentPage < 2 %}

        <li class="breadcrumb-item active">Tag &quot;{{ Tag.title }}&quot;</li>

    {% else %}

        <li class="breadcrumb-item"><a href="{{ Tag.url }}">Tag &quot;{{ Tag.title }}&quot;</a></li>
        {{ parent() }}

    {% endif %}

{% endblock %}
