{% extends 'email/base.html.twig' %}

{% block body %}

    {% include 'email/recipients-greetings.html.twig' %}

    <row>
        <columns>
            Con la presente {% if To|length == 1 %}ti{% else %}vi{% endif %} informo che ho appena <strong>pubblicato
            il seguente articolo</strong>, del quale {% if To|length == 1 %}sei autore{% else %}siete autori{% endif %}:
        </columns>
    </row>

    {% include 'email/article-link.html.twig' %}

    {% include 'email/noreply-warning.html.twig' %}

    <row>
        <columns>
            L'articolo è già visibile pubblicamente, ma ora uscirà anche in homepage e sulle pagine <em>social</em> di TurboLab.it
            <strong>{{ Article.publishedAt|format_datetime(locale='it', pattern="EEEE d MMMM y, 'ore' HH:mm") }}</strong>.
        </columns>
    </row>

    {% include 'email/user-who-acted-signature.html.twig' %}

{% endblock %}
